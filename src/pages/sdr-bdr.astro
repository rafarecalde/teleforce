---
import ServiceLanding from '../components/ServiceLanding.astro';
import { LANDINGS } from '../lib/landings';
const data = LANDINGS.find((l) => l.slug === 'sdr-bdr')!;
---
<ServiceLanding data={data} />
